# Style and Conventions

- **Language Extensions**: `OverloadedStrings` is common.
- **Imports**: Explicit export lists (`module Main ( main ) where`), qualified imports for common libraries (`Data.Map.Strict as Map`).
- **Formatting**: Spaces for `IO [ FilePath ]`.
- **Types**: `Text` is preferred over `String`.
